apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vat.fullname" . }}-test-connection"
  labels:
    {{- include "vat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
